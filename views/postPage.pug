extends layouts/main-layout.pug 

block content
    script.
        const postId = "!{postId}"
    .postsContainer

    +createCommentModal(userLoggedIn)

block scripts
    script(src="/javascript/postPage.js")